{{- if eq .machine "wsl-work" -}}
# npm globals mise auto-installs with every node it installs (so a node lts bump
# cannot lose them). One package name per line.
@anthropic-ai/claude-code
{{- end -}}
